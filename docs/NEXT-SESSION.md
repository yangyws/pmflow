# 下次從這裡繼續

2026-08-19 收工。**進度到 `CR-168`。** 兩邊 typecheck 通過、E2E **159/159**，
本地 Commit 已完成（**未 auto-push 至 main**，推送要他自己來）。

> **E2E 基準是 159**（`CR-147` 這一輪從 97 → 124 → 159）。看到舊文件寫 107 或 97 不要以為少跑了：
> `CR-127` 解鎖種類限制時移除了 10 項不再成立的案例，`CR-138` 補了 27 項排版 API，
> `CR-147` 又補了 35 項權限案例。

## 畫面驗證：使用者已在 2026-08-19 確認全部正常

這一批（`CR-131`～`CR-168`）開發期間包含：
1. **權限架構嚴格校正 (`CR-168`)**：精確切分環境變數超級管理者（全域最高）、專案擁有者（專案建立者，具備該專案最高權限與轉移特權，在工作區為一般成員）、專案管理者（`MANAGER`）與成員。
2. **全視圖警示徽章獨立折行 (`CR-167`)**：所有視圖（側欄、成員、週檢視、詳情子任務、看板、關聯圖）之警示圖示統一獨立換行展示，不再橫向擠壓變長。
3. **專案建立者與管理者身分升級 (`CR-166`)**：修復系統管理頁身分誤顯為成員 Bug，專案擁有者與管理者一律映射為 `OWNER` / `ADMIN`，解鎖「指派管理者」與完整帳號管理操作。
3. **全域警示徽章同步刷新 (`CR-162`)**：收納盒連線受阻即時警示、卡住/問題/逾期/逾回徽章全數同時並存不遮蔽、問題單免設起訖日。
4. **任務關聯圖新增文字直觀生成 (`CR-161`)**：點擊「新增文字」直接在畫布生成文字註記，不再彈出編輯視窗。
5. **同軸連線約束 (`CR-160`)**：嚴格限制左右接點連左右（排程相依）、上下接點連上下（階層關係）。
6. **系統流程圖全框體四向接點 (`CR-158`~`CR-159`)**：步驟、容器盒、區域標示框、文字註記四向自由雙向拉線與接線。
`CHANGELOG` 的狀態已全數改為已驗證。

---

## 貼給 AI 的開場白 / 接手提示詞（直接複製）

```
繼續 PMFlow 專案。請先閱讀 D:\NewProject\AGENTS.md（共用規矩）與 docs/NEXT-SESSION.md，並查看 docs/CHANGELOG.md 最上面的索引。
所有前置工作、關聯圖修復與全系統自訂 Modal 提示窗重構均已完成並完成本地 commit。
請精簡覆述我的需求後開始工作。
```

起環境（前端改檔即時生效，不用重建）：
```
cd D:\NewProject\pmflow-git
docker compose -f docker-compose.dev.yml up -d
```
改了 `apps/api` 要重建：`docker compose -f docker-compose.dev.yml up --build -d api`
網站 http://localhost:8480，示範帳號 `demo@pmflow.local` / `demo1234`

---

## 最近完成的項目（最新進度）

- **AI 技能探索端點與一鍵複製 Prompt 彈窗 (`CR-165`)**：新增 `GET /api/v1/skills` 端點回傳專案結構與完整 API 規格；在右上角選單加入「🤖 AI 串接指令」Modal，支援一鍵複製自動帶入 Token 與專案 ID 之 Prompt。
- **超級管理者環境變數與擁有者轉移 (`CR-164`)**：支援透過 `PMFLOW_ADMIN_EMAIL` 指定全域最高管理員，開放專案擁有者轉移 (`transfer-ownership`) 與帳號停用/註銷最高權限。
- **任務關聯圖並行警示即時解除 (`CR-163`)**：刪除同時關聯線時，卡片與收納盒即時解除「⚡並行」警示徽章。
- **全域警示徽章同步刷新 (`CR-162`)**：收納盒連線受阻即時警示、卡住/問題/逾期/逾回同時並存顯示、問題單免設起訖日。
- **任務關聯圖文字註記直接生成 (`CR-161`)**：點擊「新增文字」直接在畫布生成文字註記，不再彈出全螢幕對話框。
- **同軸連線約束 (`CR-160`)**：嚴格限制左右接點連左右（排程相依）、上下接點連上下（階層關係）。
- **系統流程圖接點自由連線與 Handles 統一 (`CR-158`)**：移除 `onConnect` 內部 `toOutHandle` / `toInHandle` 的強制重寫邏輯，並統一容器盒接點 ID，支援四向任意互相出發與連入。
- **任務關聯圖卡片初始給定尺寸與 measured (`CR-157`)**：卡片在第 0 幀缺少 `width/height` 與 `measured` 物件導致 React Flow 未預先計算 `handleBounds`，修復為卡片一律帶入初始 `width: 256`, `height: 90` 與 `measured`，首次進入關聯圖十字標拉線 100% 立即響應。
- **兩張圖接點標準化為 Left/Top target 與 Right/Bottom source (`CR-156`)**：設定 `isConnectableStart={true}` 與 `isConnectableEnd={true}`，註冊完整雙向接點池，支援四向任意出發與連入。
- **任務關聯圖問題單警示依畫布節點樹即時動態遞迴計算 (`CR-155`)**：在 `nodesWithHandlers` 依即時 `nodeChildrenMap` 計算子孫 `type === 'BUG'` 任務，子卡片拖出收納盒毫秒級即時歸零並移除警示徽章，並提昇卡片層級至 20~50 避免連線後十字標被 30px interaction stroke 遮蔽。

- **文件同步：解除設計稿「過時」警語 (`CR-129`)**：`ARCHITECTURE.md` 與 `SPEC.md` 已於 2026-08-12
  改版為 v3.0.0（實作現況），但 `CODEMAP.md` 與 `AGENTS.md` 的警語比它警告的東西還舊，
  害接手的人不敢讀。改成分工說明：**那兩份看「為什麼這樣設計」，找檔案一律以 `CODEMAP.md` 為準**。
- **規格補充：行動端 RWD 與 PWA (`CR-128`)**：`docs/SPEC.md` 新增第 13 章，補齊手持裝置佈局
  （寬度 < 768px 時側欄／看板／抽屜的轉換）、PWA（manifest／service worker）與 Capacitor 封裝方向。
- **任務種類上下關係全數解鎖放行 (`CR-127`)**：移除階層嵌套限制，`checkPlacement` 與 `canBeUnder`
  無條件放行，任何種類都可站最上層或掛在任何父卡片底下；`AGENTS.md` 條文與 `e2e.sh` 同步更新。
  ⚠️ 這條**推翻**了 `CR-126` 剛恢復的守門員邏輯，接手時不要照 `CR-126` 改回去。
- **清單視角型別修復 (`CR-125`)**：補回 `List.tsx` 缺失的 `DEFAULT_TYPE_COLORS` 引用。
- **靶心關聯表 (`SimpleGraph.tsx`) 五大規格重構 (`CR-124`)**：右下角固定 ↘ 縮放手把、標頭 ✏️ 鉛筆
  Hover 高亮、空位自動優先補位與手動位置右側接續網格。
- **關聯圖徹底移除移入卡片時觸發之收納盒動態拉大邏輯 (`CR-123`)**：移除佈局 `measure` 與 `boxes` 動態計算 `maxRight` / `maxBottom` 擴展邏輯，收納盒尺寸徹底 100% 靜態固定於 `384` x `288`（或使用者手動調整尺寸），卡片移入移出絕不觸發任何收納盒變大或縮放動作。
- **關聯圖移除所有自動碰撞擠開與動態推移觸發 (`CR-122`)**：徹底移除 `resolveCollisionPush` 碰撞互斥擠開邏輯與移出推移觸發，卡片拖移或放開時絕不上移/拉大/推送任何收納盒或其它卡片，僅單純進行 Menu 階層 (`parentId`) 關聯連動。
- **關聯圖卡片移出時收納盒尺寸坍塌縮放 Bug 修復 (`CR-121`)**：在佈局 `measure` 與 `place` 邏輯中鎖定容器框體基礎尺寸 `384` x `288`，解決卡片由左側（或任意邊界）拖移出框時，收納盒因內部子卡片數歸零而瞬間縮小坍塌至 264x84 之 Bug。
- **關聯圖按鈕初始標籤與狀態切換優化 (`CR-120`)**：將按鈕預設文案設定為 `📦 卡片`，按下後自動切換為 `📦 收納盒`，直觀符合「預設為卡片，按下去變成收納盒」之操作直覺。
- **關聯圖全站名詞與按鈕用語統一：事件卡片與轉換收納盒 (`CR-119`)**：統一定義關聯圖基礎節點為「事件卡片」，轉換容器模式為「收納盒」；更新 UI 按鈕文案為 `📦 轉收納盒` / `📦 收納盒`，與 Prompt Tooltips 及 Modal 視圖體驗完全一致。
- **關聯圖收納框事件卡片向右/向下移出與四向脫離判定 (`CR-118`)**：移除繪製階段根據子卡片動態擴大框體之二次計算邏輯，並採用卡片實體邊界與靜態槽位寬高 `bW` / `bH` 進行脫離檢測，卡片向右、向下、向左、向上拖出框外時皆可 100% 解除隸屬關係並平滑移出。
- **甘特圖未排期任務條自動補全與覆蓋 dhtmlx 預設綠色條 (`CR-117`)**：針對未設定起迄日之任務自動計算預設日期與 `no-dates` 虛線半透明標示，避免任務因無日期而被甘特圖過濾拋棄；同時將 dhtmlx 預設之亮綠色專案條覆蓋為 PMFlow 主題深藍/靛藍配色。
- **全頁籤 Menu 選取同步與亮藍外框高亮 (`CR-115` & `CR-116`)**：為看板 (`Board`)、行事曆 (`Calendar` / `Week`)、對外詢問 (`Inquiry`)、成員 (`Members`) 與儀錶板 (`Dashboard`) 補全 `focusedTaskId` 支援，實現自動定位捲動與亮藍外框/發光效果，七大視圖頁籤全數 100% 雙向同步 Menu 選取。
- **關聯圖收納框動態尺寸計算徹底鎖定與四向移出 (`CR-114`)**：排除 layout 中使用 draggedOffsets 計算框體大小之邏輯，鎖定收納框尺寸為靜態槽位，徹底解決拖曳卡片時推大收納框之問題，卡片往右/向下拖移可 100% 順暢移出框外脫離。
- **修正側欄 active 選取判定與選取藍底留存 (`CR-113`)**：修正 `EpicSidebar` 的 `active` 條件與高亮底色，使側欄項目獲藍色背景框與粗體，無論切換至哪一個視圖頁籤（關聯圖、甘特圖、清單、看板、行事曆），Menu 事件選取底色皆 100% 完好留存。
- **修復甘特圖選取反白文字對比 Bug (`CR-112`)**：重構 `index.css` 的 `.gantt_selected` 樣式，指定明晰對比的背景底色與文字顏色，徹底解決選取事件列時預設反白導致文字看不到之問題。
- **甘特圖主事件與大項目彙總呈現修復 (`CR-111`)**：引入 `rollup` 彙總起迄日與 dhtmlx `type: 'project'`，解決 Menu 主事件/大項目因自身無獨立日期而被微過濾掉之問題，完好呈現在甘特圖中。
- **區分單擊高亮選取與雙擊/鉛筆圖示詳細編輯 (`CR-110`)**：點擊清單與側欄僅進行選取與視圖置中高亮（不被 TaskDrawer 全螢幕遮蓋），雙擊清單行或點擊 ✏️ 鉛筆圖示方會開啟編輯頁面。
- **雙向連動導覽與 Menu 祖先自動展開 (`CR-109`)**：實作右側各視圖點擊事件時反向同步左側 Menu 藍字高亮，且左側 Menu 自動沿著 parentId 鏈條遞迴展開所有祖先父節點。
- **左側 Menu 點擊連動右側視圖與獨立 ✏️ 鉛筆編輯 (`CR-108`)**：點擊側欄事件跟隨右側目前頁籤（關聯圖聚焦單一事件、甘特圖自動捲動高亮、其餘視圖連動選取），點擊 ✏️ 鉛筆圖示專門觸發編輯抽屜。
- **關聯圖收納框動態膨脹徹底修復與四向移出 (`CR-107`)**：修正 layout/place 的收納框尺寸計算邏輯，將框體邊界鎖定為靜態槽位，徹底解決拖曳卡片時推大收納框之問題，卡片往右/向下拖移可 100% 順暢移出框外。
- **關聯圖卡片右側與下側脫離機制修復 (`CR-106`)**：重構收納框脫離判定為雙階段中心點計算，解決向右或向下拖移超越收納框邊界時無法順暢脫離之 Bug。
- **統一全系統「事件卡片」用語稱呼 (`CR-105`)**：全面將「事件框」更名為「事件卡片」，包含 Modal 對話框與系統說明，消除與收納框之用語混淆。
- **關聯圖收納框脫離機制修復 (`CR-104`)**：修復拖曳框內事件卡片時收納框動態膨脹問題，改用靜態位置計算容納尺寸，卡片超越實體邊界可順暢移出框外脫離。
- **行事曆工具列佈局對齊與無縫切換 (`CR-103`)**：重構 [月視角 | 週視角] 為單一層工具列最左端切換鈕，切換月/週視角時按鈕、標題與導覽控制完全原地靜止不跳動。
- **行事曆整合週檢視 (`CR-102`)**：採用方案 A，將週檢視整合進「行事曆」頂部 `[ 月視角 | 週視角 ]` 切換鈕，功能一體化並簡化全域頁籤列。
- **成員視圖整合未分派事件 (`CR-101`)**：採用方案 A，將未分派事件置頂整合於「成員」頁左側選單，點擊可即時切換與指派負責人，不重複佔用頂部全域頁籤。
- **甘特圖移除關鍵路徑統計提示列 (`CR-100`)**：移除頂部「關鍵路徑共 X 個節點」視覺列，畫面更加精簡乾淨。
- **甘特圖關閉互動編輯轉純唯讀視圖 (`CR-099`)**：關閉拖曳改期、端點拉線建立與點擊刪除依賴，移除相關提示與 Modal，專注於純資料展示。
- **甘特圖左右區塊滾輪分流 (`CR-098`)**：游標置於左側任務清單控制上下垂直捲動，置於右側時間軸進度條控制左右水平捲動。
- **重構全系統原生 alert/confirm 為自訂 UI Modal 提示窗 (`CR-097`)**：全系統取代瀏覽器原生對話框，對齊 TaskDrawer 規範之高質感 Modal 提示窗。
- **關閉收納框內部事件卡片碰撞互斥避讓 (`CR-096`)**：於 resolveCollisionPush 加上 parentId !== null 直接 return 判定，允許框內卡片自由擺放。
- **重構 BoxNodeView 與 TaskNodeView 為 justify-start (`CR-095`)**：將內部資訊列頂部固定對齊，徹底消除切換收納模式時內部標題與進度條上下伸縮跳位 Bug。
- **修復 getTypeColor 未優先使用自訂種類顏色 Bug (`CR-094`)**：修正 Graph.tsx 優先採用傳入之 customColor，實現關聯圖與全系統種類顏色及名稱 100% 同步。
- **對齊收納框內部事件相對縱向偏移量 (`CR-093`)**：統一 layout 與 onNodeDragStop 的框內 y 軸起點為 y >= 60（避開標頭），消除佈局重算時內部卡片跳位 Bug。
- **MRG 後方種類徽章渲染與修復框內拖移彈射 Bug (`CR-092`)**：編號後方渲染種類徽章且顏色與文字對齊種類，並引進 parentOverrides 解決框內拖移彈射消失問題。
- **關閉收納模式二次確認與內部事件移出 (`CR-091`)**：關閉收納時檢查框內子事件，彈出對話框提示「收納框內尚有 X 個事件框，關閉收納模式將會把內部事件框移出至外部畫布，確定要關閉嗎？」，確認後平滑移出。
- **修復 isEpic 硬性強制判定與補齊關聯線 API 刪除 (`CR-090`)**：修正 isEpic 舊有只要有子任務就強制作廢為大項目的 Bug，並補齊圖上按 Delete 鍵同步 DELETE API 刪除連線。
- **關聯圖徹底清除大項目徽章標籤 (`CR-089`)**：清除所有節點上殘留之「大項目」徽章標籤與邏輯條件，維持圖面乾淨精簡。
- **修復事件框外框顏色覆蓋並移除大項目徽章 (`CR-088`)**：移除預設灰框 class 讓種類顏色真實呈現於外框，並移除頂部大項目圖像徽章標籤。
- **關聯圖事件框配色對齊種類顏色 (`CR-087`)**：重構 getTypeColor，使大項目/任務/問題/里程碑之框線與頂條配色全系統統一對齊。
- **收納框穿透離框、側欄顏色連動與 EDITOR 保存權限 (`CR-086`)**：解鎖 nodeExtent 允許卡片穿透框線，側欄顏色即時同步，EDITOR 角色開放保存所有事件。
- **解鎖收納框內手動拖曳卡片位置限制 (`CR-085`)**：手動拖放保持精確座標（吸附 24px 網點），預設移入維持自動 Slot 補位。
- **修復收納框內部事件卡片全向 4 邊離框 Bug (`CR-084`)**：完成重構當前父框上、下、左、右 4 邊實體邊界離框解鎖（含 24px 容錯），卡片向任何方向拖移出框皆可順暢脫離。
- **端對端 API 測試 107/107 全數通過 (`CR-083`)**：對齊 CR-066 依賴規則與補齊資料庫預設初始連線，API E2E 測試 100% PASS。
- **寫入 API 測試長期規範 (`CR-082`)**：於 AGENTS.md 寫入「修改 API 必須同步執行 E2E 測試」規範。
- **側欄選單收納框統一置頂與橫向分隔線 (`CR-080`)**：完成設定 Menu 左側選單中收納模式開啟之事件框統一排在最高最高處，並於下方渲染精緻分隔線。
- **收納框智慧缺位優先填補與手動佔位避讓 (`CR-079`)**：完成實作 Smart Slot Occupancy Engine，移出的空位下次移入自動填補，手動移動佔位則順延避開至下一個空位。
- **修復收納框內部移入移出留空 Bug (`CR-078`)**：完成設定收納框內部卡片強制採用 place 排版，移出時剩餘卡片自動向上緊密補位，移入時自動填補，零空洞。
- **修復右側移入避讓方向誤判彈跳 Bug (`CR-077`)**：完成重構避讓方向向量為相對幾何中心（diffX/diffY），從右側向左拖時，左側卡片精確順暢向左避開。
- **碰撞排斥避讓機制排除收納框 (`CR-076`)**：完成設定收納框不被拖曳卡片排斥推移，事件卡片可順暢放入收納框。
- **單擊切換選擇/多選藍框維護與群組齊推 (`CR-075`)**：完成注入 selected 狀態至卡片 data 屬性，點擊第一次選取高亮、第二次取消，依序點擊即可全部選取。
- **修復連續單擊多選遭重置 Bug (`CR-074`)**：完成移除預設單選重置覆蓋，可流暢連續單擊卡片 1、2、3... 進行多選。
- **移除原本單擊卡片時彈出的資訊提示窗 (`CR-073`)**：完成移除 Focus Panel 彈窗，單擊專注於多選高亮與群組移動。
- **單擊多選/雙擊開啟/多框齊移 (`CR-072`)**：完成設定雙擊開啟詳情頁、單擊切換多選高亮，並支援按住任一被選取卡片帶動全體一起移動。
- **移出收納框卡片優先置於游標放點（或佔用時置於旁邊） (`CR-071`)**：完成設定移出收納框卡片精確停留在游標釋放處，若位置被佔用則順延放在該框旁邊。
- **關聯圖框與框碰撞自動方向擠開避讓 (`CR-070`)**：完成實作動態碰撞推移引擎，依據拖曳位移向量將被重疊框體向反方向自動擠開與對齊網格。
- **修復移入新卡片既有卡片跳動與第 9 個卡片無法放入 Bug (`CR-069`)**：完成改為穩定索引排序（新卡片追加於末端）並擴大 MARGIN=96px，既有卡片完全不跳動且第 9 個卡片順暢放入。
- **修復收納框第 7 個（第 2 欄）卡片離框誤判 Bug (`CR-068`)**：完成修正 `onNodeDragStop` 多欄動態邊界讀取與 48px 容錯 Margin，第 7 個以上卡片皆能穩定收納。
- **關聯圖收納框內部卡片拉開進度條距離 (`CR-067`)**：完成調整 `BOX_HEADER = 96px`，拉開子事件卡片與進度條之間的留白距離。
- **大項目與一般任務關聯全面解鎖 (`CR-066`)**：完成解除後端與前端大項目（EPIC）與一般任務之間建立排程（FS/SS/FF/SF）與語意依賴之限制。
- **關聯圖收納框內所有事件框與框間距統一為 24px (`CR-065`)**：完成設定收納框內橫向與縱向框間距一律為 24px，與 4 邊內距 100% 一致對齊 24px 網點。
- **關聯圖收納框內部卡片每欄上限 5 張，滿 5 張向右開第二排 (`CR-064`)**：完成設定收納框內單欄最多放 5 張卡片，滿 5 張自動向右開啟第 2 欄繼續向下延伸。
- **關聯圖收納框內部卡片預設垂直向下延伸排列 (`CR-063`)**：完成重構收納框內部卡片指派邏輯，卡片加入框內預設一律向下垂直堆疊。
- **關聯圖卡片放進收納框自動擴大、移出保持原尺寸 (`CR-062`)**：完成設定卡片放入框內空間不足時自動拉大包覆，卡片拖移離框時收納框維持尺寸不變。
- **關聯圖卡片移出收納框不連動拉大 (`CR-061`)**：完成移除拖曳卡片時收納框擴大追蹤，卡片可順暢拖出收納框並解除隸屬關係。
- **關聯圖收納框內部卡片拖曳邊界連動擴大 (`CR-060`)**：完成修復 `isBox` 判定含 `containerBoxIds`，內部卡片向右/向下拖曳時收納框即時自動擴大包覆。
- **關聯圖卡片外框配色一致化 (`CR-059`)**：完成設定卡片外框線條顏色 (`borderColor`) 與事件主題顏色 (`accentColor`) 保持 100% 一致。
- **關聯圖卡片與容器框 24px/48px 網點完全吸附 (`CR-058`)**：完成設定卡片尺寸 288x96px 與容器框 24px 寬/48px 高倍數吸附，四角、邊框與 Handle 100% 精確壓在背景 Dot 網點上。
- **關聯圖 100% 進度條翡翠綠一致化 (`CR-057`)**：完成設定 `progress === 100` 時進度條線條顏色統一轉為翡翠綠 `#10b981` (`bg-emerald-500`)。
- **關聯圖縮放對齊背景網點 (`CR-056`)**：完成手動拉伸與自動算出的框寬高一律吸附對齊背景 24px Dot 網格一格。
- **關聯圖收納框內部四邊等距對齊 (`CR-055`)**：完成統一內部事件卡片上(對齊標題列)、下、左、右四周 24px 完全相等邊距。
- **關聯圖卡片偶數高度對齊規則 (`CR-054`)**：完成實作 `even(v)` 強制全圖卡片與容器框 `height` 均收斂為偶數像素，確保 Handle 中點 (`height / 2`) 零浮點小數。
- **關聯圖收納開關配色與進度條一致化 (`CR-053`)**：完成統一頂部標籤條顏色 (`getTypeColor`) 與進度條通用元件 (`NodeProgressBar`)，切換收納開關配色與進度條視覺 100% 保持一致。
- **關聯圖收納框卡片拖曳與排除 Bug 修復 (`CR-052`)**：完成修正空間階層換算 `getAbs` 遞迴父層座標與即時框尺寸，拖放於框內不再被誤設為 `parentId = null`。
- **關聯圖卡片一致尺寸與標題位移修正 (`CR-051`)**：完成非收納狀態卡片尺寸統一，限制僅收納模式可調整大小，並將標題固定於標號正下方第二行。
- **關聯圖收納與縮放按鈕位置調整 (`CR-050`)**：完成將「收納」按鈕移至卡片編號 (MRG) 左側，非收納狀態自動隱藏右側「縮放」按鈕。
- **關聯圖卡片按鈕文案正名 (`CR-049`)**：完成將卡片按鈕「尺寸」改為「縮放」，「獨立」改為「收納(關)」，點擊啟用後切換為「收納(開)」。
- **多子代理並行長期規則 (`CR-048`)**：依使用者指示寫入 `AGENTS.md` 長期規則：只要任務可拆分且檔案不衝突，直接自動開啟多子代理平行處理，無須每次詢問。
- **對外詢問回覆日單行佈局與 +幾天 選項 (`CR-047`)**：完成將 `InquiryTable.tsx` 的期望回覆日重構為不換行單行佈局，`+1 天` / `+3 天` 等選擇連動即時更新日期。
- **行動端 RWD 與 Mobile/PWA 規格補充 (`CR-128`)**：已於 `docs/SPEC.md` 新增第 13 章，補齊手持裝置佈局 (RWD)、PWA 支援與 Capacitor 跨平台行動端規劃。
- **全部功能打包推送 main (`CR-041`)**：完成將 OAuth 登入、請假代理人、成員頁面板等共 37 個異動檔案 Commit 並 Push 至 main。
- **發版批次檔 release-0.2.2.bat (`CR-040`)**：完成產出 `release-0.2.2.bat`，可直接雙擊發版構建 `v0.2.2`。
- **進度輸入框 % 符號顯示 (`CR-039`)**：於 `TaskDrawer.tsx` 的進度輸入框右側加上 `%` 標籤。
- **事前分析檔名經同意才動手 (`CR-038`)**：將「先查 CODEMAP 分析改動檔案，列出清單等使用者同意才動手」寫入 `AGENTS.md`。
- **已完成任務鎖定僅管理者可改 (`CR-037`)**：將「已完成任務僅專案建立者(管理者)可修改」之正式條文寫入 `AGENTS.md`。
- **清單視角功能規範 (`CR-036`)**：將 `List.tsx` 完整功能與規則寫入 `D:\NewProject\AGENTS.md` 長期規則與 `CODEMAP.md` 中。
- **已完成任務鎖定與權限限制 (`CR-035`)**：完成已完成任務鎖定（僅管理者可改）與開單人/負責人/代理人編輯權限後前端連動控管。
- 目前無待辦事項，上三項已全數確認與完成。

- **全權授權核准（長期規則）**：更新 `AGENTS.md` 規範，確認全權授權自動推進與執行所有檔案修改、指令與 commit/push。
- **查索引與行數/字數限制（長期規則）**：寫入「查 CODEMAP 動局部、對話回覆 100 字內、代碼修改單次 100 行內」規範至 `AGENTS.md`。
- **異動索引編號與註解規範（長期規則）**：`CHANGELOG.md` 索引與條目全數改用 `CR-001` ~ `CR-033` 編號，程式碼內清理長註解改留 `Ref: CR-xxx` 引用，並已寫入 `AGENTS.md`。
- **頁面功能對應索引（長期規則）**：已在 `docs/CODEMAP.md` 新增完整「頁面與功能對應清單」索引表格，並依使用者交代將「接手任務先查 CODEMAP 確定對應頁面」寫入 `D:\NewProject\AGENTS.md` 長期規則中。
- **今天與選中天視覺區分**：將「今天」改為專屬鮮紅圓章（`bg-red-600`），與藍色選中日期／拖曳目標形成強烈對比，一眼即可辨識。
- **行事曆拖曳位移修正**：將 `DragOverlay` 改用 `createPortal` 掛載至 `document.body`，解決容器滾動與下方任務拖曳時預覽框錯位問題。
- **關聯圖點對點平行直線**：排程依賴與匯合點 Edge type 改為 `straight` 模式，點對點水平對齊時呈 100% 直通直線平行向右穿越。
- **週檢視單日標籤優化**：移除重複的「看整週」按鈕，將「只看 M/D」改為附帶 `✕` 的點擊式徽章標籤，點擊直接復位全週。
- **關聯圖齊平對齊**：關聯鏈分群依 `upstreamY` 升序對齊，強制相連的點跟點完全置於相同 Y 軸高度，向右延伸呈整齊水平線。
- **工作規矩（規則檔寫入策略）**：交代的任務預設均為單次任務，完成後寫入 `docs/CHANGELOG.md` 即可；只有明確交代「記起來 / 這是長期規則」時才寫入 `AGENTS.md`。
- **關聯圖佈局方向**：關聯鏈（FS/SS/FF/SF）自動對齊上游 Y 座標平行向右延伸，僅上下階層與非排程相關（RELATION）才向下延伸。
- **週檢視單日切換**：點擊頂部一週七天日期可切換至單日視角篩選當日任務，工具列提供單日標籤與「看整週」復位按鈕。
- **頁籤順序調整**：設定彈窗改用 `dnd-kit` 可拖曳排序（`≡` 手勢），移除了箭頭按鈕。
- **行事曆操作說明**：長說明改為懸停圖示提示（`ⓘ 操作說明` + `title` tooltip），解決擠壓工具列與按鈕換行的問題。
- **側欄新增子任務**：點擊 `＋` 按鈕自動展開父節點，就地輸入建立合法種類之子任務。
- **關聯圖 SS/FF 繪製**：確認兩端有連接任務時畫圓點，無連接時退回直條連線（`hub === false`），防止空頭圓點。
- **WEB 專案 AAA (WEB-1) 層級**：確認 WEB-1 在資料庫中已為 EPIC 大項目且位於最上層，通過 `fix-task-levels.bat` 驗證無違規。
- 儀表板（燃盡圖＋負載熱圖，M4 收尾）、任務種類的上下關係、大項目與任務之間
不能有排程依賴、沒回完的對外詢問不能結案、側欄變成真的樹（種類色＋錯／外／逾）、
任務詳情改成按保存才送出並加上刪除、「發文追蹤」改叫「對外詢問」、
「問題」改叫「錯誤」、清單捲不動、週檢視收合與依類型分組。
細節看 `docs/CHANGELOG.md` 最上面那幾條。

## 兩支可以重複執行的批次（在專案根目錄）

- `clean-demo-junk.bat` —— 清點來點去留下的測試任務（軟刪除）。
- `fix-task-levels.bat` —— 把站在最上層的任務搬到大項目底下並重建 closure。

兩支都**跑第二次不會出事**（已驗）。只做猜得到答案的事，其餘列出來不動。

- **熱圖人名固定 (`CR-045`)**：已將 `WorkloadHeatmap.tsx` 左側成員欄改為 `sticky left-0` 固定容器。
- **關聯圖排程依賴說明 (`CR-046`)**：已於 `Graph.tsx` 底部說明列新增四種排程依賴懸停詳細說明。

## 已知的小債

- 熱圖在工時模式下示範資料預估時數填寫較大，工時模式示範數據呈現偏深色區間。

## 別忘了

- 規矩全在 `D:\NewProject\AGENTS.md`，**唯一一份、所有 AI 共用**
  （`CLAUDE.md` 與 `GEMINI.md` 只有一行 `@AGENTS.md`）。
  新規則**當場**寫進 `AGENTS.md` 再動工 —— 上下文一壓縮就只剩檔案裡的東西。
- 每批改動寫進 `docs/CHANGELOG.md`（索引一列＋詳細條目一段）
- 會再跑第二次的東西寫成可以重複執行的批次
- 發版的 bat 交出去就停手，等他回覆。推 tag 才會發版，推 main 只跑 CI

---

## 2026-08-05 這一整天做完的

**上午那批**（收尾前一天的 wip）：深色模式與文案外移收完、請假接上、專案設公開、
按不動的按鈕不畫出來、任意帳號都能開專案（開的人就是管理者）、
管理者可搜尋任意帳號拉人、發文追蹤移進專案、成員移到頭像選單。

**下午那批**：
- **每個專案自己的系統參數頁**（`0011_project_parameters.sql`：優先度與任務類型
  從寫死的 CHECK 拆成兩張表）。可改名、改色、拖曳排序、刪除時要指定搬到哪裡去。
- **任務轉派**：獨立端點，換人＋交接說明一起寫進活動紀錄，通知鈴鐺也看得到那句話。
- **週檢視**：這一週有哪些任務在跑、各卡在哪個狀態。
- **頭像顯示修好**（`<img src>` 不會帶授權標頭 → 改成 fetch 拿 blob）。
- **行事曆的勾選記得住**（`lib/remember.ts`）。
- **甘特圖深色補完**：列的底色其實一直是白的，還有一批 dhtmlx 自己畫的淺灰。
- **關聯圖五件**：大項目的框可以拉大小、匯合點可以拖、「語意關聯」改叫
  「任務相關」而且改走上下連線、說明列不再自己重複自己、
  **不會再把你放大看的地方拉回全景**。
- 順手抓到一個一直都在的 bug：**關聯圖的 ＋／－ 按鈕根本沒作用**
  （`useReactFlow` 的 `zoomIn/zoomOut` 在這個畫面上是靜悄悄的沒反應），
  改成自己讀 viewport 再寫回去。

## 2026-08-05 第三次開工：上面那批「待驗證」的拿滑鼠驗過了

驗過而且都會動：**系統參數頁拖曳排序**（拖完重新整理仍在、看板欄位順序跟著換、
↑／↓ 也能一格一格移）、**關聯圖框拉大小**、**匯合點拖曳**、**相關類走上下連線**、
**重新排列會把拖過拉過的全部歸位**。

驗的過程抓到三個 bug，都修掉並推上 main（見 CHANGELOG
「清單擠成一直條、框選不起來、圓點抓不到」）：清單的任務名稱被擠成一個字一行、
大項目的框選不起來（所以拉大小從來沒生效過）、匯合點的圓點太小又被框蓋住抓不到。

## 2026-08-05 第四次開工：儀表板（M4 最後一項）

- **儀表板頁籤**：燃盡圖 + 負載熱圖，兩張都自己刻 SVG，沒有加任何相依。
  拿滑鼠驗過：兩種計算單位、十字準星、每一格的提示框、表格版、深淺兩種模式。
- **活動紀錄本來回推不了歷史**（看板拖曳完全沒留紀錄），三個寫入點都補了。
  舊資料查不到轉換紀錄的那幾張，完成時間用 `updated_at` 估，畫面上會講出來
  有幾張是估的 —— **那句話不可以拿掉**。你現在的 dev 資料庫是 3 張。
- **週檢視**：分組可以收合（記得住）、可以改成依類型分組、每列補上類型徽章。
- **清單捲不動**修掉了（外層沒給高度，捲軸從來沒生出來過）。

## 還沒在畫面上驗過的

- 代理提醒過的幾個點：框的把手在深色下的對比、上下 handle 會不會太顯眼、
  相關類的邊在密集區會不會跟框的邊框打架（把手實際出現之後看起來還可以，
  但沒有在很密的圖上試過）
- 示範資料庫裡有幾張手動測試留下的重複任務（MRG-9～MRG-14 有兩組同名的），
  不是程式的問題，但看畫面時容易誤會

## 還沒動工

- **沒有了。M4 全部做完。** 下一步**由他指定，不要自己挑一件開始做。**

## 決定不做的

- **逾期提醒信**（2026-08-05 他說不用）。理由與「不要順手加寄信套件」寫在
  `D:\NewProject\AGENTS.md` 的「刻意不做的事」。

## 已知的小債

- 說明列去重之後，四種排程依賴的**長說明**（「上游做完的隔天下游才動得了」那幾句）
  在畫面上沒有地方顯示了。這是照「線上有字就不再講一次」的原則走的結果，
  要留的話得另外找地方掛。
- 系統參數頁刪除時的「還有 N 張在用」下限，是拿自動佈局算出來的尺寸當框的最小值；
  如果先把子任務拖到框外面很遠，框還是縮得到蓋住它們。
- 熱圖在工時模式下，示範資料幾乎整片都是紅的（每天滿載線 8 小時，示範任務的
  預估時數填得大）。不是程式的問題，但色階等於用不到，看起來會嚇一跳。
- 熱圖左邊的人名欄不是 sticky。整張是單一 SVG，橫捲時人名會捲出畫面；
  要 sticky 就得把人名拆成 HTML，會犧牲跟格子的對齊。

## 別忘了

- 規矩全在 `D:\NewProject\AGENTS.md`，**唯一一份、所有 AI 共用**
  （`CLAUDE.md` 與 `GEMINI.md` 只有一行 `@AGENTS.md` 指過去）。
  新規則寫進 `AGENTS.md`，**不要另外開檔，也不要只存進某個工具自己的長期記憶**
  —— 換一個工具就等於沒交代過。（2026-08-05 改，原本是寫在 `CLAUDE.md`）
- 每批改動寫進 `docs/CHANGELOG.md`（索引一列＋詳細條目一段）
- 發版的 bat 交出去就停手，等他回覆。推 tag 才會發版，推 main 只跑 CI
